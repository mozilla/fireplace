@import "lib";

[data-page-type~="app-list"],
[data-page-type~="brand-landing"],
[data-page-type~="collection-landing"],
[data-page-type~="homepage"] {
    .compat-filter {
        display: block;
    }
}

.compat-filter {
    display: none;
}
