@import 'lib';

.no-results {
    padding: 20px;
    text-align: center;
}

.search-results-header b {
    font-weight: 400;
}

.search-results-header .app-list-filters-expand-toggle {
    position: relative;
    top: -2px;
}

#search-results {
    margin-top: -3px;
}

@media $base-desktop {
    #search-results {
        margin-top: 0;

        .listing > li {
            .shots .next,
            .shots .prev {
                opacity: 0;
            }
            &:hover {
                .shots .next,
                .shots .prev {
                    opacity: 1;
                }
            }
        }
    }
}
