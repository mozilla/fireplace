@import 'lib';

#page .account-settings {
    padding: 15px;
    > .toggles:first-child {
        margin-bottom: 20px;
    }

    .form-footer {
        text-align: center;
        button {
            margin-bottom: 1em;
        }
    }
}

#feedback-overlay section {
    width: 450px;
}
